@import '/static/styles/variable.wxss';

page {
  background-color: var(--color-background);
  width: 100%;
  font-size: 32rpx;
  font-family: PingFangSC;
}

.container {
  font-size: var(--font-size-medium-x);
  line-height: 1.5;
}
